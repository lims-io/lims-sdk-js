{
  "name": "@lims.io/sdk-node",
  "version": "0.1.0",
  "description": "LIMS Server SDK for Node",
  "private": true,
  "engines": {
    "node": ">=6.11.5",
    "yarn": ">=1.0.0"
  },
  "scripts": {
    "dev": "babel -w src -s -D -d dist",
    "prebuild": "yarn clean:dist && mkdir -p dist",
    "build": "babel -w src -s -D -d dist > /dev/null",
    "clean:dist": "rimraf dist",
    "clean:src": "rimraf src",
    "test": "jest --watch --config .jestrc.json",
    "lint": "esw -w src test"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0"
  },
  "devDependencies": {
    "babel-eslint": "^8.2.2",
    "babel-jest": "^22.4.1",
    "babel-plugin-module-resolver": "^3.1.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "eslint": "^4.18.1",
    "eslint-plugin-import": "^2.9.0",
    "eslint-plugin-jest": "^21.12.2",
    "eslint-plugin-promise": "^3.6.0",
    "eslint-watch": "^3.1.3",
    "jest": "^22.4.2",
    "mocha": "^5.0.1",
    "rimraf": "^2.6.2",
    "sinon": "^4.4.2",
    "supertest": "^3.0.0"
  }
}
