{
  "name": "lims-sdk-js-wrapper",
  "version": "0.1.0",
  "description": "Monorepo for the LIMS JavaScript SDK",
  "private": true,
  "engines": {
    "node": ">=6.11.5",
    "yarn": ">=1.0.0"
  },
  "scripts": {
    "dev": "lerna run --parallel --scope @lims.io/* dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
